This is the true reads annotation that comes from the results of mapping the reads to the gold-standard reference database by the most optimal mapping tools--trim+bbmap
Then replace the sequence id with ncbi taxon id
01_taxon_id.sh 01_taxon_id.py
